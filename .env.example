# Database Connection - Neon DB (Vercel Integration)
# Only DATABASE_URL is needed for Vercel deployment with Neon DB
DATABASE_URL=postgresql://your-neon-connection-string-here

# These are only needed for local development or non-Neon deployments
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=councilinsight

# Maxun Integration - Use either API key OR username/password
MAXUN_URL=https://your-maxun-instance-url
# Option 1: API Key Authentication (recommended)
MAXUN_API_KEY=your_api_key_here
# Option 2: Username/Password Authentication
#MAXUN_USERNAME=admin@example.com
#MAXUN_PASSWORD=adminpassword

# Node Environment
# This will be set automatically by Vercel in production
NODE_ENV=development
PORT=3000

# Vercel-specific environment values
# These will be set automatically by Vercel
#VERCEL=1
#VERCEL_ENV=production
#VERCEL_URL=your-app.vercel.app
#VERCEL_REGION=cdg1