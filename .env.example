# Database Configuration
DATABASE_URL=postgresql://user:password@endpoint:port/councilinsight

# Maxun Integration
VITE_MAXUN_URL=https://maxun-service-url.run.app
MAXUN_USERNAME=optional_username
MAXUN_PASSWORD=optional_password
MAXUN_API_KEY=optional_api_key

# Google Cloud Storage (for Maxun service)
GCS_BUCKET_NAME=councilinsight-data

# Google Vertex AI (Optional, for AI features)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Security Settings
SESSION_SECRET=your-session-secret
JWT_SECRET=your-jwt-secret

# Deployment Settings
PORT=8080
NODE_ENV=production